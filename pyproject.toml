[project]
name = "test-gonfleur"
version = "0.1.0"
description = "Pressure controller desktop application"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pyqt5>=5.15.11",
    "pyvisa>=1.16.2",
    "pyvisa-py>=0.8.1",
    "zeroconf>=0.149.16",
]

[project.scripts]
pressure-controller = "main:main"
